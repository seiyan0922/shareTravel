package controller

import "net/http"

func EditHandler(w http.ResponseWriter, r *http.Request) {
	// p, err := LoadPage(title)

	// if err != nil {
	// 	p = &Page{Title: title}
	// }
	// RenderTemplate(w, "edit", p)
}
